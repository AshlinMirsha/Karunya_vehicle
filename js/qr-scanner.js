import { supabase } from '../supabase/client.js'; import { renderNavbar } from '../components/navbar.js'; import { showToast } from '../components/toast.js';
export async function initCheckinPage() { const { data:{ user } }=await supabase.auth.getUser(); if(!user) return location.href='/'; renderNavbar(user,'Student Check-In'); const token=new URLSearchParams(location.search).get('token'); if(token) document.getElementById('input-token').value=token; document.getElementById('btn-submit-checkin').onclick=async()=>navigator.geolocation.getCurrentPosition(async p=>{ const {data,error}=await supabase.functions.invoke('attendance-api',{body:{action:'mark-attendance',token:document.getElementById('input-token').value.trim(),latitude:p.coords.latitude,longitude:p.coords.longitude}}); if(error) return showToast(error.message,'danger'); if(data?.message) showToast(data.message,'success'); },()=>showToast('GPS permission is required.','danger'),{enableHighAccuracy:true,timeout:10000}); }
